import { Request, Response } from 'express'

export const createImmobileController = async (
  request: Request,
  response: Response
): Promise<Response> => {
  return response.json()
}

export const listImmobileController = async (
  request: Request,
  response: Response
): Promise<Response> => {
  return response.json()
}